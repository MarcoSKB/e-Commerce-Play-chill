import React from "react";
import { Section } from "@/src/components/modules";

const CartPage = () => {
  return <Section title={`Cart ${0}`} children={undefined}></Section>;
};

export default CartPage;
