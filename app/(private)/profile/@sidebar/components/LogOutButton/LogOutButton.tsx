"use client";
import { ProfileButton } from "../../../components/elements";

export default function LogOutButton() {
  return (
    <li className="mt-auto">
      <ProfileButton onClick={() => {}} color="red">
        Log out
      </ProfileButton>
    </li>
  );
}
